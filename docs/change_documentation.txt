1) Добавить FlowMVI
2) Разнести архитектуру на фичи:
    2.1) Уже есть существующая фича: профиль (модуль profile)
    2.2) Уже есть существующая фича: Home (но внутри card-feature), ее надо вынести в модуль home
    2.3) Фича образы и деталка образов в модуле outfit (такого модуля нет пока)
    2.4) Фича гардероба и деталка гардеробов в модуле wardrobe (такого модуля нет пока)
    2.5) Внутри фичи профиля будут входить такие фичи: Экран Размеры, Экран Профиль, Остальные экраны текущих настроек
    2.5) Фичи конструктора надо вынести в отдельный модуль (outfit_constructor)
3) Системные компоненты мы вынесем в core/compose/components
4) Навигацию будет строить с Navigation 3 последней версии (стабильной) так как они Multiplatform. BottomSheetNavigation удаляется из проекта, так как она не нужна будет по факту. Все экраны будут открываться с одной точки входа (можем оставить AppNavHost) - это для диплинков
5) Текущую реализацию Deeplinks перекинуть в core в модуль deeplink
6) Строить каждый модуль согласно архитектуре FlowMVI + Clean architecture, core модуль необязательно
7) Platform.kt вынести в core модуль
8) App.kt вынести в feature main модуль (главный модуль для всех)
9) composeApp -> androidApp думаю надо переименовать, чтобы было понятнее что это точка входа для платформы android
10) MainViewController непонятно зачем находится в composeApp - если не используется на ios стоит удалить
11) card-feature - RootScreen переносим в main модуль, deeplink в core -> deeplink, навигацию в модуль main в подмодуль navigation; это все выносим и удаляем card-feature модуль
12) recommendations-page-screen оставляем как есть, но не используем
13) tiktok-feed удаляем отовсюду
14) Фишки из wardrobe-feature распихиваем в utils в модуле core, репозитории и тому подобные классы (di, use case) в другие логичные модули нашей архитектуры соблюдая clean architecture + flow mvi
15) Давай с тобой отдельно пропишем документацию по clean architecture + flow mvi в отдельном файле
16) Навигация вроде NavGraphBuilder.wardrobeNavGraph должна быть в каждой фиче отдельно (именно навигация фичи). Продумай как это втиснуть в нашу архитектуру architecture + flow mvi